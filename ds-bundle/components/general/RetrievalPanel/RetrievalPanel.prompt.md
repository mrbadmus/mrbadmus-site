RetrievalPanel from mrbadmus-3d-studio. Use via `window.MrBadmusDS.RetrievalPanel` (bundle loaded from the root `_ds_bundle.js`).

## Examples

### RoundInProgress

```jsx
() => (
  <div
    style={{
      width: 404,
      height: 660,
      display: 'flex',
      padding: 16,
      background: '#15110C',
      borderRadius: 14,
    }}
  >
    <RetrievalPanel specimen={specimen} targetIndex={2} roundSize={6} />
  </div>
)
```
