LibraryColumn from mrbadmus-3d-studio. Use via `window.MrBadmusDS.LibraryColumn` (bundle loaded from the root `_ds_bundle.js`).

## Examples

### EightSpecimens

```jsx
() => (
  <div
    style={{
      width: 232,
      height: 620,
      display: 'flex',
      padding: 12,
      background: '#FBF3E6',
      borderRadius: 12,
    }}
  >
    <LibraryColumn selectedId="heart" onSelect={() => {}} />
  </div>
)
```
