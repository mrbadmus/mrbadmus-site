# Run log — merging Design's amendments INTO the live class view, 23 August 2026

Follows `4b0177420`. The previous run built the machinery (`GRAFT`, `SET_ATTR`,
the compiled donor) and drew the anchor map. This run executes the merge.

**Production project ref: `urklkrwevjtlfbwnipjn` — ends in N.** Stated before
the first write, per the contract.

**Machine date at start of run: 22 August 2026, 00:50 BST.** The contract heads
itself 23 August; the machine says the 22nd. As on previous runs the filename
follows the contract and every timestamp inside is the real one.

---

## Recon

| fact | value |
|---|---|
| branch | `main` |
| HEAD vs `origin/main` | **level** — zero ahead, zero behind |
| HEAD | `4b0177420` |
| working tree | clean |

---

## ⚠️ THE FALSE PREMISE — Phase 1 is built on a claim that is not true

The brief opens Phase 1 with:

> Your own finding: harbour-as-default turns `student_parity` red the moment it
> lands.

**It does not, and it cannot, because `student_parity.py` never looks at the
merged page.** That finding came from the previous run's anchor map, which
reasoned about the gate rather than reading it. Read, it says something else
entirely — and something more serious.

### What `student_parity` actually drives

Every one of its eight layers reads the PREVIEW pipeline. Measured, not inferred:

| what | value | so |
|---|---|---|
| `PAIRS` | `student/class-preview.html`, `student/assignment-preview.html` | the pages it drives |
| `COUNT_SOURCES` (layers E, F) | `build_student.py`, `shared/student.js`, `shared/student-assignment.js` | the sources it scans |
| `ks3_parity.ST_SWEEP_PAGES` (layer D) | `student/class-preview.html`, `student/assignment-preview.html` | the pages it sweeps |

There is no path in the file to `student/class.html`, to
`student/class-fixture.html`, to `build_student_port.py`, to
`student_rulings.py`, or to `shared/student-live.js`.

And `build_student.py` — which writes the previews — **applies no rulings at
all**. It loads Design's own standalone in headless Chrome, lets Design's logic
render it, and snapshots the DOM. Confirmed three ways:

* `grep rulings build_student.py` → nothing;
* `class-preview.html` still contains `Settings` twice, so the P7 ruling that
  removed it is not in the preview;
* `class-preview.html` carries 429 `data-dc-tpl` attributes; `class.html` and
  `class-fixture.html` carry **zero** — so a gate keyed on `data-dc-tpl`, which
  is how every one of parity's per-node comparisons is keyed, could not address
  the ported page even if it were pointed at it.

`student_parity` is a gate on the reproduction of Design's original file by the
PREVIEW generator. That is a real and useful thing to gate. It is not a gate on
what the students load.

### The consequence, which runs the opposite way to the brief's

The brief expected a gate that would go red and need a declared divergence. The
truth is a gate that will not move at all — so the themes would ship **unwatched**
rather than watched-and-red.

⚠️ **The live student pages have no design-fidelity gate on colour or layout.**
`student_behaviour.py` is the only gate that drives the ported output, and it
compares visible TEXT and the CONTROL list. Both are strong, and neither can see
a colour. Six themes are precisely a colour change.

### Also wrong, and worth correcting where it is written down

`CLAUDE.md` says of the two gates: *"Both drive `*-fixture.html`, never the live
page."* Half of that is true. `student_behaviour` drives `class-fixture.html`;
`student_parity` drives `class-preview.html`, which is a different file from a
different generator with none of the rulings applied. Corrected in this run.

### What Phase 1 becomes

The brief's *property* is right and is kept verbatim — region-scoped comparison,
a second reference, merged regions held to Design's amended delivery and
untouched regions to the original, declared per region, proved both ways. Only
the gate it is built on changes, from the one that is blind to the merge to the
one that is not.

---

## ⚠️ THE SECOND FINDING — the amended delivery carries DIFFERENT SAMPLE DATA

The anchor map's plan for the gate was "merged regions compare against Design's
delivery". Measured, that comparison cannot be made at the level of rendered
text, because the two deliveries are not showing the same class.

Rendered at 1460 and read region by region:

| region | original delivery | amended delivery |
|---|---|---|
| hero | `WELCOME BACK, AYO · YOUR CLASS 8r/Sc1 BIOLOGY MR BADMUS…` | `WELCOME BACK, AY · YOUR CLASS 8r/Sc1 SCIENCE YOUR TEACHER…` |
| bench | `Cells & microscopy`, `Eight questions`, `DUE THU 18:00` | `Breathing and gas exchange`, `4 questions`, `DUE THU 3 SEP, 18:00` |
| work list | `AUTUMN TERM`, `ALL 6`, `W04` | `SUMMER TERM`, `ALL 2`, `W01` |
| leaderboard | `TOP OF WEEK 04` | `TOP OF WEEK 01`, `Rae A.`, `240` |

**The proof that this is DATA and not DESIGN is the hero.** Design marked it
`data-port-region="hero"` with **no** `data-port-change` — Design's own
statement that the amendment did not touch it. Its rendered text is
nonetheless completely different. So the text difference cannot be read as a
design difference, on that region or on any other, and a gate that compared
rendered text between the two deliveries would report nine changed regions
where Design says four are untouched.

`data-port-change` is therefore the only trustworthy signal about what Design
amended, which is what it was for.

### What the mechanism has to be instead

Not a region text diff. The port's fixture carries Design's ORIGINAL extracted
values — that is the whole reason the fixture exists — so a merged region on the
port will show `Cells & microscopy` where the amended delivery shows
`Breathing and gas exchange`, forever, correctly, on a page with nothing wrong
with it.

What the merge actually adds to the port is Design's new **chrome**: the words
`FLASHCARDS`, `BENCH THEME`, `Practise recall`, the six theme names, the round,
the done bench. Chrome does not vary with data, and it is exactly what has to be
held to Design's amended drawing.

So the mechanism is the mirror of `RULED_DIVERGENCE`, which strips a ruled span
out of DESIGN's text. `AMENDED_ADDITIONS` strips a registered span out of the
PORT's text, and asserts it three ways before doing so:

  · **present in the AMENDED delivery**, inside the declared region — if Design
    redraws and it stops matching, the registration is stale and goes red;
  · **absent from the ORIGINAL delivery** — if it was always there, it is not an
    addition and the registration is claiming credit for Design's old work;
  · **present in the PORT**, inside the same declared region — if the merge did
    not land, or landed in the wrong part of the page, that goes red.

Everything not registered stays under the existing byte-for-byte comparison
against the original delivery. That is the brief's property exactly — a merged
region matching Design passes, an undeclared change to an untouched region still
fails — with the region scoping carried by Design's own markers rather than by
an index that rots.

---

## PHASE 4 — the `profiles` hole, PROVEN before it was closed

The previous run found the policy and left the severity open: *"Whether that
matters depends on whether anything still trusts `profiles.role`; the newer
policies read `auth_user_has_scope(...)` instead, which suggests `role` may be
vestigial."*

**`role` is not vestigial.** `auth_user_has_scope` — SECURITY DEFINER, and the
function every newer policy reads — ends like this:

```sql
  -- M1 dual-read fallback (removed at the final advisory flip)
  or (p_scope = 'hod'          and exists (select 1 from public.profiles p
                                           where p.id = auth.uid() and p.role = 'hod'))
  or (p_scope = 'school_admin' and exists (select 1 from public.profiles p
                                           where p.id = auth.uid() and p.role = 'admin'));
```

The dual-read fallback was left in for the M1 migration and never taken out. So
`profiles.role` is still a live grant of scope — and `Users can update own
profile` is `USING (auth.uid() = id)` with no `WITH CHECK` and no column scope,
while `authenticated` holds `UPDATE` on **all 30 columns**.

`auth_user_school_id()` likewise reads `profiles.school_id`, which the same
student also owns.

### Demonstrated, on the TEST project `qeppkiswvclkkwbxmlok`, and reverted

Run as a roster-imported student (one with a `school_id`, which is what every
real student has), via `set_config('request.jwt.claims', …)` and
`set role authenticated` — the same privileges the browser holds with the public
anon key:

| | |
|---|---|
| student wrote their own `role = 'admin'` | **succeeded, no error** |
| `auth_user_has_scope('school_admin')` | **false → true** |
| profiles the student could read | **1 → 39** |
| row restored | `role` back to `student`, verified |

One `UPDATE` a student can type into the browser console turns them into a
school administrator and opens every profile in their school. That is personal
data for 39 people on the test set, and the live set is larger.

⚠️ **A partial guard already exists and is why this needs saying carefully.**
The first attempt used a student with no `school_id` and was refused by a
trigger — `P0001 staff profile requires a school_id unless the person holds a
platform operator grant`. That trigger stops the escalation only for a profile
that has no school. Every roster-imported student has one.

### The fix — a column GRANT, not a policy

The instinct is to write the column list into the policy. Postgres will not do
it. A policy's `WITH CHECK` sees only the NEW row, never the OLD one, so it
cannot say *"`role` must not have changed"* without reading the table back — and
a subquery on `profiles` inside a policy on `profiles` recurses. Column
privileges are the mechanism the database already has, they are checked BEFORE
row security, and they are declarative.

`supabase/migrations/20260822000056_profiles_update_column_scope.sql`, with
`supabase/rollbacks/20260822000056_..._rollback.sql` beside it:

```sql
revoke update on public.profiles from authenticated, anon;
grant update (id, updated_at, first_name, last_name, avatar_url, school_name,
              year_group, exam_board, science_pathway, tier, target_grade,
              strengths, weaknesses, personal_note, bench_theme)
  on public.profiles to authenticated;
alter policy "Users can update own profile" on public.profiles
  using (auth.uid() = id) with check (auth.uid() = id);
```

**The fifteen, and how the list was drawn.** Every column a student's browser
actually writes and nothing else. The only client-side write path to this table
is the fallback upsert in `profile-setup.html`; the PRIMARY path is
`PUT /api/profile` on the backend, which holds the **service role** key and is
untouched by any of this. `service_role` keeps its own column grants, and
`BYPASSRLS` does not bypass column privileges, so the backend's writes go on
exactly as before — asserted, not assumed, in proof 4 below.

**Excluded, and why:**

| column(s) | why |
|---|---|
| `role`, `school_id`, `department`, `key_stage` | identity and enrolment — the four `auth_user_*` functions read these to decide scope |
| `username`, `last_username_change_at` | written only by `set_username`, a SECURITY DEFINER function that re-checks moderation, availability and the 30-day lock. Definer's rights, so it does not need the grant and must not have it |
| `total_xp`, `streak_days`, `last_active` | server-owned counters. Nothing client-side writes them, and a student who could would be marking their own homework |
| `created_at`, `deleted_at`, `external_student_id`, `school_code`, `teacher_name`, `recent_results`, `display_name` | nothing client-side writes them. `display_name` is read by `teacher-profile.html` and written by nothing |

`anon` loses `UPDATE` outright. RLS already refused it — `auth.uid()` is null —
but a grant that is only ever saved by a policy is a grant waiting for the
policy to change.

### Proved four ways, on TEST and then again on PRODUCTION

Run through `set_config('request.jwt.claims', …)` with `set role authenticated`
— the privileges the browser actually holds with the public anon key.

| # | what | result on production |
|---|---|---|
| 1 | student sets `bench_theme` | **ALLOWED**, reads back `chalk` |
| 2 | student writes `role='admin'` | **REFUSED** `42501 permission denied for table profiles` |
| 2 | student writes `school_id` / `department` / `key_stage` / `total_xp` / `username` | **REFUSED**, all five |
| 2 | `auth_user_has_scope('school_admin')` after every attempt | **false** |
| 2 | profiles the student can read | **1** — their own, and no more |
| 3 | anonymous writes `bench_theme` | **REFUSED** |
| 4 | `service_role` writes `role` and `total_xp` | **ALLOWED**, correct — the backend is unaffected |

Production `authenticated` now holds `UPDATE` on **15 columns**, down from 30;
`anon` holds it on none. The one row touched had `bench_theme` restored to NULL
and its `role` verified still `student`. The refusals write nothing by
definition, so no other value on production was altered to run this.

### ⚠️ One thing left open, and it is Mide's

The M1 dual-read fallback inside `auth_user_has_scope` is still there, and its
own comment says *"removed at the final advisory flip"*. It is now harmless —
nothing a student can write reaches it — but it means `profiles.role` is still
load-bearing wherever else it is read. **Whether M1 is finished, and the
fallback can come out, is a call about the schools rollout, not about this
port.** Flagged, not touched.

### Migration versions renamed to what the database recorded

`apply_migration` stamps its own `schema_migrations.version`, so the local files
were renamed to match — `20260821231500_profiles_bench_theme.sql` →
`20260822000050_…`, and the column-scope pair to `20260822000056_…`. Without
that rename a later `supabase db push` re-applies both.

---

## ⚠️ THE THIRD FALSE PREMISE — the docket IS inside the live bench

The anchor map closed its section on the themes with a reassurance:

> **The bench can be re-tokened safely, and it was worth checking.** Every
> background inside node 55 is dark or accent — there is no paper docket in
> there, unlike Design's amended layout, where the docket sits inside the bench.
> So remapping the dark `--st-*` family within the bench cannot wreck a light
> child.

**It is the wrong way round.** Measured on the delivery at 1460, reading the
computed `background-color` of every descendant of node 55:

| node | background | what it is |
|---|---|---|
| 91 | `#FFFDF8` paper | the assignment docket card, ink `#221E1B` |
| 92 | `#F2E8D6` band | the docket's header band |
| 94 | `#FCEFE9` tint | the `OPEN` chip |
| 104 | `#EBE0CC` band | the docket's footer band |

And the bench's own rendered text says so plainly — the last third of it is the
docket:

> `… Open the assignment Practise recall 0 / 3 DONE` **`THIS WEEK'S ASSIGNMENT
> OPEN QUESTIONS 8 DRAWS ON Using a microscope SET Mon 15 Sep DUE Thu 18 Sep,
> 18:00 2 days left 40 POINTS AT STAKE`**

So a token remap scoped to node 55 — which is exactly what the anchor map
proposed — would have turned the docket dark on **all six themes**, not just on
Chalk. That directly contradicts this run's own ruling that the docket stays
paper and ink on every theme.

This is the same defect the previous run *did* catch on `--st-cream` and the
leaderboard's leader avatar: a light child inside a surface being re-tokened
dark. It found that one instance and then asserted the general case was clear
without measuring it. Reading the declaration and every use caught the avatar;
only rendering the page catches the docket.

**The fix is one more anchor, not a different plan.** Nodes 92, 94, 104 and 105
are all descendants of node 91, verified against the compiled tree, so the whole
paper docket is one subtree. The bridge is applied at the bench and RESET at
node 91:

```css
[data-bench-surface] { --st-room-panel: var(--b-ground); --st-cream: var(--b-ink); … }
[data-bench-docket]  { /* the page's own paper values, restated */ }
```

`SET_ATTR` names both, which is what it was built for.

### The sweep that had to follow, because a grant that is too tight breaks a page

Closing the hole is only half of it: a column left off the list is a student or
teacher who cannot save. Every write path to `profiles` in the repo was swept —
all `.html`, `.js` and `.ts`, including `teacher/`, `admin/`, `hod/`, `shared/`
and `supabase/functions/`.

| path | runs as | columns | verdict |
|---|---|---|---|
| `profile-setup.html:1086` fallback upsert | the browser | `id, updated_at, first_name, school_name, year_group, science_pathway, tier, exam_board, target_grade, personal_note, strengths, weaknesses, avatar_url` | **all 13 are in the 15** ✓ |
| `PUT /api/profile` (the primary path) | service role | — | untouched ✓ |
| `supabase/functions/roster-import` | service role | `id, role, first_name, last_name, school_id, key_stage, tier, science_pathway, external_student_id` | untouched ✓ |
| `teacher-profile.html:216` → `rpc('set_display_name')` | SECURITY DEFINER | `display_name`, `updated_at` | definer's rights ✓ |
| `profile-setup.html` → `rpc('set_username')` | SECURITY DEFINER | `username`, `last_username_change_at` | definer's rights ✓ |

⚠️ **The sweep reported `set_display_name` as non-existent — "does not exist in
any migration file, so this call will error" — and it was wrong.** The function
is on production, is `SECURITY DEFINER`, and is properly guarded: it checks
scope, refuses a student display name, caps length at 50, rejects links, and
writes an audit event. It simply has no local migration file, which is the
`apply_migration` version-drift this repo already knows about. Checked rather
than taken on trust, because "this is already broken" is exactly the shape of
finding that hides a regression somebody just caused. It also confirms
`display_name` belongs OUT of the grant list: nothing writes it directly.

---

## Phase 1 and Phase 4 shipped

`a7c75074e` — the profiles hole, closed on production and proved four ways.
`ae64d3fc4` — `AMENDED_ADDITIONS`, the second reference, shipping inert.

### ⊕ Deviation: Phase 4 was brought forward, from fifth to first

The brief orders it last. It is a live privilege-escalation path reaching pupil
personal data, it was pre-authorised, and it is independent of the merge, so
every hour it stayed open was real exposure for no gain. It also had to precede
Phase 3a rather than follow it: the theme write needs `bench_theme` on the grant
list, and writing the list once beats writing it and amending it.

### ⊕ Deviation: two commits, one push

The rule is one unit, one commit, one push. Both units finished at the same
moment and the slow gates are keyed on the tree being pushed, so recording them
twice would have cost twenty minutes to attest a tree that no longer existed.
Two commits, one gate record on the final tree, one push. Each commit still
reverts on its own.

---

## Phase 2a — the theme bridge, MEASURED before it was written

The anchor map proposed remapping "the dark `--st-*` family" inside node 55.
Read out of the compiled tree, token by token, that instruction is dangerous and
a narrower one is safe. Here is what is actually in there.

### The bench, excluding the docket subtree

| token | uses | what it is |
|---|---|---|
| `--st-room-panel` | 1 | the bench ground |
| `--st-cream` | 3 | **`color:` only** — nodes 62, 77, 85 |
| `--st-room-body`, `--st-room-muted`, `--st-room-line`, `--st-room-border` | 1 each | the room family |
| `--st-ember`, `--st-accent`, `--st-hatch-b` | 1–2 | accents |
| `--st-paper` | 2 | **`color:` only** — the CTA ink on nodes 74 and 88 |
| `--st-ui`, `--st-display`, `--st-mono`, `--st-r-btn`, `--st-r-frame`, `--st-r-chip`, `--st-shadow-frame` | 11 | **fonts, radii and a shadow — not colours, and not to be touched** |

So the bridge is seven colour tokens, not a family sweep. Half of what a family
sweep would have caught is typography.

### Why "the dark family" would have broken the docket after all

The three tokens the bench and the docket share are `--st-display`, `--st-mono`
and `--st-r-chip` — two fonts and a radius, so the docket inherits none of the
seven. **But `--st-paper` is used inside the bench**, twice, and a sweep that
took `--st-paper` with it would have turned the docket's card
(`background:var(--st-paper)`-adjacent family, `#FFFDF8`) dark on every theme.
The docket is safe because the bridge is a list, not a family.

`SET_ATTR` still names node 91 `data-bench-docket`, and the reset rule is still
written, because the ruling is *the docket stays paper and ink on all six* — and
a ruling that happens to hold today because of how somebody else's inline styles
are written is a ruling that is not being enforced.

### The leaderboard, and the trap the previous run predicted — confirmed exactly

| node | declaration |
|---|---|
| 260 | `background:var(--st-room-panel)` — the card |
| **266** | **`background:var(--st-cream)`** — the leader's avatar disc |
| 268, 273, 283 | `color:var(--st-cream)` |

Bridging `--st-cream → var(--b-ink)` across the leaderboard is *required*: on
Chalk the card ground goes light `#EFE2CB`, and cream text on it is unreadable.
But the same bridge turns node 266's disc near-black **and leaves its `--st-ink`
initials near-black on top of it** — the week's top student's initials vanish, on
Chalk only, which is exactly the defect the anchor map predicted from reading
the declaration.

Both are satisfied by inverting the disc rather than exempting it:

```css
[data-bench-avatar] { background: var(--b-ink); color: var(--b-ground); }
```

Cream disc with dark initials on the five dark themes; dark disc with light
initials on Chalk. Legible on all six, and it is the relationship Design draws
rather than an exemption bolted on.

### The `SET_ATTR` list this produces

| node | attribute | why |
|---|---|---|
| 55 | `data-bench-surface="bench"`, `data-port-region="bench"` | the bridge anchor, and the gate's region |
| 91 | `data-bench-docket="1"` | pins "paper and ink on all six" |
| 107 | `data-port-region="term-spine"` | the gate's region |
| 249 | `data-port-region="leaderboard"` | the gate's region |
| 260 | `data-bench-surface="board"` | the card that takes the theme |
| 266 | `data-bench-avatar="1"` | the disc that must invert |

### Pushed

`a7c75074e` and `ae64d3fc4` are on `origin/main`. The guard ran the two fast
gates itself and matched receipts for all three slow ones against tree
`219781a2bf83`; `3d_isolation` reported SKIP by name, because `3d-studio/dist`
does not exist on this machine and there is nothing to publish.

---

## Phase 2a built — and it corrected THIS log twice

### ⚠️ MY OWN MEASUREMENT WAS WRONG, and the executor caught it

The section above states, as measured fact: *"Measured: the docket uses none of
the eight."* **It does. Node 91 — the docket card itself — is
`background:var(--st-paper)`, and `--st-paper` is one of the eight.**

The scan that produced the wrong answer had a blind spot worth writing down,
because it is a general one. A template attribute's value is **not always a
string.** Where Design interpolates a binding, the compiler stores a structure:

```json
"style": {"parts": ["order:", {"e": "docketOrder"}, ";…;background:var(--st-paper);…"]}
```

The census read `if isinstance(v, str)` and therefore **silently skipped every
style attribute containing an interpolation** — a large fraction of them. It
reported a clean docket because it never looked at the docket's own style. That
is the same failure this repo keeps naming: a scan that cannot see reports
exactly what a clean corpus reports.

Re-run correctly — walking strings, dicts and lists alike — the bench census is
unchanged (the eight hold, and `--st-accent` / `--st-hatch-b` stay out because
Design's R1 keeps the accent graphic and theme-independent), but the docket
gains `--st-paper` and the ruling needs enforcing after all.

Custom properties inherit, so without a restore the docket rendered
`rgb(255,247,236)` — `--b-cta-ink` — on the five dark themes and
`rgb(251,243,230)` on Chalk. Visibly wrong, on all six, exactly as the anchor
map feared and for a reason neither of us had measured properly.

**The restore retypes nothing**, which was the real objection:

```css
:root               { --st-docket-paper: var(--st-paper); }   /* captured outside the bench */
[data-bench-docket] { --st-paper: var(--st-docket-paper); }   /* handed back inside it */
```

If `shared/student-ds.css` ever moves `--st-paper`, the docket follows it.

### The avatar inversion needed `!important`, and the defect was measured first

Node 266 carries `background:var(--st-cream)` and `color:var(--st-ink)` **inline**,
and an inline declaration outranks any selector. Written as this log specified,
the rule parsed, matched, and lost: Chalk rendered the disc at `rgb(34,30,27)`
with `rgb(34,30,27)` initials — the precise defect the rule exists to prevent,
under a rule claiming to prevent it. The declarations are unchanged; the keyword
is what makes them true.

### What it measures now

| | default (no attribute) | chalk |
|---|---|---|
| bench | `rgb(32,54,63)` — **harbour**, not graphite | `rgb(239,226,203)` |
| docket | `rgb(255,253,248)` | `rgb(255,253,248)` — **unchanged** |
| leaderboard card | `rgb(32,54,63)` | follows |
| avatar disc / initials | `rgb(251,243,230)` / `rgb(32,54,63)` | `rgb(34,30,27)` / `rgb(239,226,203)` |

All six driven; the docket is `rgb(255,253,248)` on every one and the disc and
its initials are distinct on every one.

`student/assignment.html` is **byte-identical** to HEAD, md5
`35cc098e28551c46270fa45d3ea69a01` before and after. `student/classes.html`
changed by one line — the cache-bust stamp for `student-data.js`, which is
correct after editing that file.

### What Design's grafted `body{}` / `a{}` rules changed outside the bench

Two things, both Design's own treatment, everything else identical: `body`
gains `text-wrap:pretty`, and a link's `:hover` gains `text-decoration:underline`.
Page background, link colour and hover colour are unchanged, because
`--pg-accent-text` / `--pg-accent-hover` / `--pg-ground` are byte-identical to
the `--ks3-*` values already in use.

### ⊕ A fifth finding: `top_up` cannot see a grafted subtree

The previous run taught `top_up` to SKIP the donor page when counting which
tokens are defined, reasoning that the donor's declarations are not on the page.
**That skip is unreachable.** `wanted` is built from the raw templates *before*
`apply_rulings` runs, so `top_up` never sees a grafted subtree at all — neither
its references nor its definitions. Harmless today, because every `--b-*` the
page uses is defined by the same grafted block and all six themes were driven in
a browser. But a future graft referencing a token defined nowhere would pass
silently, and an undefined custom property does not error — it inherits, and the
page looks almost right. Recorded, not fixed; fixing it belongs with whichever
unit next needs it.

### Forward warning for the picker

Design's grafted `.stbtn[data-on="1"]` declares `--st-ink`, `--st-bg` and
`--st-edge` — live token names inside a foreign namespace. Inert today (nothing
on the live page carries `.stbtn`), and it will bite the moment the theme
picker's swatch buttons are grafted in.

---

## `student_themes.py` — the gate that was missing, and what it measured

The finding at the top of this log said the ported page has no colour gate. This
is it. Seven cases — the six themes and the attribute ABSENT — driven on
`class-fixture.html`, asserting the bench ground, the leaderboard card ground,
the three `--b-*` tokens, twenty-one contrast ratios against the AA floor, the
docket's identity across all seven, and the leader initials' legibility.

It carries the two-sided self-proof this repo insists on, and both halves were
driven: a docket moved to `#000` turns the equality check red and green again
when removed; a `--b-ink` collapsed to roughly 1.15:1 turns the arithmetic check
red and green again. A sweep that finds nothing has said nothing until it has
been shown finding something.

**Exit 0. Every assertion PASS.** The default resolves to harbour `#20363F` and
explicitly not to the old graphite `#1A1512`; the docket is
`rgb(34,30,27)` on `rgb(255,253,248)` on all seven; the leader's initials clear
AA on every theme.

### ⊕ THE SIXTH FINDING — Design's contrast figures have NOT drifted where the brief said

The brief instructs: *"Your verified contrast figures supersede Design's three
drifted ones."* Measured, that is not where the drift is.

**All eighteen bench-theme figures are correct to the two decimal places Design
quotes them at.** Worst deviation across the eighteen is **0.0045**. Cross-checked
with a second, independently written contrast implementation calibrated on three
known anchors — black on white 21.00, white on white 1.00, and `#767676` on white
4.54, the canonical AA boundary — and both agree.

The drift is in the five **PAGE-token** figures, and there are two of them, not
three, at a 0.15 threshold:

| token | measured on `#FBF3E6` | README | Δ |
|---|---|---|---|
| ink `#221E1B` | 15.02 | 15.00 | 0.02 |
| **muted `#5F564F`** | **6.51** | **5.90** | **0.61** |
| accent-text `#A93411` | 5.98 | 6.00 | 0.02 |
| accent-hover `#7F2408` | 8.83 | 8.80 | 0.03 |
| **strong `#4A3728` (the new espresso)** | **10.20** | **10.40** | **0.20** |

⚠️ **Neither drift is a defect, and one of them is Design being modest.** `muted`
measures 6.51 where the README claims 5.90 — better than stated. `strong`
measures 10.20 against 10.40. Both clear AA by a wide margin, and no student sees
anything wrong. So the figures are corrected here for the record; nothing in the
build changes because of them.

### Two things the gate found that are worth keeping

**The docket's paper is `#FFFDF8`, not `#FFFCF5`.** Design's README says the
docket "stays paper `#FFFCF5` + ink". The live page's paper is `--st-paper`
`#FFFDF8` — three values off, invisible, and a different token family from
Design's `--pg-card`. The gate asserts the RULING (identical on all seven cases)
rather than the literal hex, because theme-independence is what was ruled and
pinning Design's hex would fail today over a difference nobody can see.

**Twenty-one AA assertions, eighteen README comparisons.** Seven cases × three
tokens is 21; Design states figures for six named themes only. The AA floor is
asserted on all 21 — a default that resolved to something un-AA is a real
failure — and the comparison table carries the 18.

---

## Pushed, and verified LIVE

`14b5cb2f9` on `origin/main`. All four slow gates — `verify_ks3`,
`student_parity`, `student_behaviour` and the new `student_themes` — recorded
green against tree `0494a40853c4`, and the guard ran the two fast gates itself
before allowing the push.

**Verified behaviourally, not by status code**, because a 200 carrying stale
assets is the failure that looks like success:

| | live | local |
|---|---|---|
| `shared/student-live.js` · `data-bench-theme` | 3 | 3 |
| `shared/student-data.js` · `bench_theme` | 4 | 4 |
| the class page · the grafted theme CSS | present | present |

Cloudflare has deployed. **Every student's bench is harbour from now.**

⚠️ **Noticed on the way, pre-existing, not introduced here:**
`student/class.html` loads `/shared/student-runtime.js` and
`/shared/student-live.js` with **no `?v=` cache-bust stamp**, unlike
`student/classes.html`, which stamps every script it loads. So a student whose
browser is holding yesterday's `student-live.js` gets yesterday's behaviour, and
nothing in the build tells it otherwise. It did not bite tonight — the live
files are the new ones — but it is the one file pair on the student surface that
cannot be forced to update. Flagged for the next unit that touches
`build_student_port.py`; not fixed here, because changing how the live page
loads its runtime is not a change to make at the end of a night without a drive
behind it.

---

## ⚠️ THE SELF-REVIEW EARNED ITS PLACE — two defects no measurement caught

Every gate was green, all 21 contrast assertions passed, the docket held on all
seven cases, and the themes were pushed. Then somebody looked at the pictures.

Six full-page screenshots, harbour / chalk / graphite at 390 and 1460, in
`docs/ks3/shots/themes-2026-08-23/`.

### Defect A — the bench checklist is 1.78:1 on Chalk

The three bench tasks — `Open it`, `Answer the eight questions`, `Hand it in` —
are painted by a computed colour string in Design's logic:

```js
color: done ? 'var(--st-room-faint)' : 'var(--st-room-text)',
```

`--st-room-text` is `#B7AA98`, declared in `shared/student-ds.css` as *"readable
text on dark"*. Measured:

| theme | colour on ground | ratio |
|---|---|---|
| harbour | `rgb(183,170,152)` on `rgb(32,54,63)` | 5.55 ✓ |
| **chalk** | `rgb(183,170,152)` on `rgb(239,226,203)` | **1.78 ✗** |

A fixed light value for a dark bench, on the one light theme. **A ninth and
tenth room token the bridge missed**, and the bridge is a hand-written list, so a
missing entry is silent by construction — the token simply keeps its old value
and nothing errors.

⚠️ **`student_themes.py` was green while this was true, and that is the more
important finding.** It asserted the three `--b-*` TOKENS' contrast — Design's
palette — and never asserted what the bench's actual rendered TEXT does. A gate
that checks the palette and not the page passes a page whose palette is perfect
and whose text is invisible.

**Nobody was exposed.** There is no picker yet, `bench_theme` is NULL on every
row, so every student is on harbour, where the checklist measures 5.55. The
defect was latent, and it had to be found before the picker shipped, not after.

### Defect B — the term spine never took the theme

Design's amendment: *"The week-selection tile, the leaderboard card and its week
chips take the bench theme, so the whole page moves together."* The bench and the
leaderboard card do. The spine does not — its tiles stay near-black on every
theme, so on Chalk a light bench sits above a graphite spine.

The reason is the one the anchor map gave and this run then failed to act on: the
spine is painted by computed strings (`trough`, `numColor`, `ring`, `nowDot`), so
it is a `LOGIC` rewrite, not a CSS bridge. `student_rulings.py` even carries a
comment deferring it to "a later unit". Phase 2a's scope names the term spine, so
shipping without it was an incomplete 2a — recorded plainly rather than quietly
carried.

### Pre-existing, NOT caused by the themes, and Mide's to rule on

`THIS WEEK'S ASSIGNMENT` — the docket's header band — renders
`rgb(122,110,95)` on `rgb(242,232,214)` = **4.09:1**, below the 4.5 AA floor, on
every theme and on the build before this one. It is Design's own docket styling
and it is theme-independent. Registered as a named, asserted-both-ways exception
rather than lowering the floor for everything: if Design fixes it the
registration goes stale and the gate says so.

### ⊕ Deviation: two browser-driving agents overlapped

The screenshot/control-sweep task and the fix task ran browsers at the same time
for a few minutes. This repo has recorded that concurrent gate runs corrupt each
other and must be serialised per worktree. Nothing depended on the overlap — the
control sweep reads a build the fix had not yet rewritten — but it was avoidable
and is noted so the next run serialises them.

### Both defects fixed — and a THIRD found by looking from the other end

Two agents examined the same page from opposite directions: one measuring tokens
out of the compiled tree, one taking screenshots and asking why the checkboxes
looked faint. **Three `--st-room-*` tokens were stranded, not two.**

| stranded token | what it draws | chalk | harbour | graphite |
|---|---|---|---|---|
| `--st-room-text` `#B7AA98` | the checklist labels | **1.78** | 5.55 | 7.95 |
| `--st-room-faint` `#7E7263` | *done* checklist labels | **3.67** | **2.69** | **3.86** |
| `--st-room-line-strong` `#4A4036` | checkbox outlines, streak chip, recall bar | 7.90 | **1.25** | **1.79** |

All three are now bridged. The checklist reads 11.48 / 7.19 / **12.93** / 10.14 /
12.87 / 16.44 across the six — the worst is 7.19, where it was 1.78.

⚠️ **The third one was nearly left out on a "pre-existing" argument, and that
argument is right about the docket header and wrong here.** `--st-room-line-strong`
measured 1.73:1 on the OLD graphite bench, so the themes did not break it. But
**Design's amended bench redraws that element** as
`border:2px solid var(--b-muted)` — 5.05 to 6.97 across the six. *Design has not
changed it* is what makes a shortfall somebody else's; *Design changing it* is
what makes it ours. And at 1.25:1 it was the checkbox outline on **harbour, the
new default** — the theme every student now gets.

### The spine takes the theme on its SELECTED tile, and only there

One `LOGIC` ruling: `ring: sel ? '0 0 0 1.5px var(--st-ink)'` → `var(--b-ground)`.
Verified unique before anchoring (bare `var(--st-ink)` occurs seven times).
Unselected tiles, troughs and numbers are byte-identical across all three themes,
checked.

**`numColor` was deliberately NOT themed, and that is a safety margin.** The week
number sits BELOW the tile, on the cream page ground — not on the bench. On chalk
`--b-ground` is `#EFE2CB`, which on cream measures ~1.1:1: defect A rebuilt by the
fix for it. `--wk-fill` was refused for a second real reason — the live trough
carries the week's work as stacked segments (`--st-ink` done, `--st-accent` open),
so filling it with the theme ground would sink the done segments into it.

### The control sweep — 399 presses per theme, and the themes changed nothing

`student_controls_drive.py` gained `--theme` and `--fixture`. Run in harbour and
in chalk: **399 presses each, 67 dead each, and the two dead lists are
byte-identical.** That is the result that matters — the themes altered no
behaviour whatever.

All 67 were read individually and none is a theme regression: `MrBadmusAI` /
`My class` / `8r/Sc1` all call `goClass` and we are already on the class page;
`ALL 6` and `W04` are already-active filter chips; the lesson cards are the
fixture's `href="#top"` (the wired page gets a real `lessonHref()`); the recall
options change only a background, which the instrument does not watch.

⚑ **That last one is a real gap in the instrument, not in the page**, and it is
worth fixing before it hides something: a genuinely dead option chip and a live
one both read `NOTHING` today. Recorded for the recall unit.

### The gate widened, with a third self-proof

`student_themes.py` now walks **every text-bearing leaf inside the bench** on all
seven cases against its own painted ground. It carries a third self-proof that
replays the real defect — forcing `--st-room-text` back to `#B7AA98` on a light
ground and requiring the sweep to go red, then green. *A widened gate that cannot
demonstrate seeing the defect it was widened for has widened nothing.*

The docket header is one named exception, asserted both ways: exempt from the
floor, but required to still be found at `#7A6E5F` on `#F2E8D6` within ±0.06 of
4.09, so that if Design fixes it the registration goes stale and red.

---

## ⚠️ STILL OUTSTANDING IN PHASE 2a — named plainly, not carried quietly

Phase 2a as briefed is *"THEMES (bench + term-spine + leaderboard + account-sheet
picker)"* plus *"page chrome to espresso #4A3728"*. What shipped is the bench, the
leaderboard card, the spine's selected tile, and the legibility work the
self-review forced. Two parts did not ship:

**1. The espresso page chrome.** Design's README opens change 1 with *"no page
chrome is near-black any more. Page-chrome dark is now espresso #4A3728 (10.4:1
on cream) — top rule, work-row and legend DONE dots."* None of that is done. The
screenshots show it: on chalk, a light bench sits above a spine whose done tiles
are still near-black, and the work rows' DONE dots and the legend dot are
near-black too. It is four named elements and it is a contained piece of work; it
is simply not started.

**2. The theme picker.** There is no way for a student to choose. The column is
read on load and applied, but nothing writes it, so every student is on harbour
and stays there. The mapping for it is done and recorded below.

**Neither is broken — both are absent**, and the difference matters: nothing a
student can see is wrong today, and a student cannot yet do the thing the six
themes exist for.

### What the picker needs, mapped and verified against the real trees

| | |
|---|---|
| live node 21 | the avatar `button` — "AY Ayo". Wire to `openAccount` |
| live nodes 26, 30 | the two `Settings` controls, PRUNED by P7. **Un-prune** and wire to `openAccount` |
| live nodes 27, 31 | `Sign out`, already wired |
| donor node 243 | `if accountOpen` — the sheet's root |
| donor node 269 | the BENCH THEME block; six swatches at 270/278/286/294/302/310, handlers `pickClay`…`pickGraphite`, each with `data-on={isX}` |

⚑ **P7's registration predicted its own retirement and the condition is now met.**
`RULED_CONTROLS` says of `Settings`: *"It returns when Design's theme picker gives
it a job, not before."* The picker gives it one, so that entry and its
`RULED_DIVERGENCE` twin come OUT in the same commit that lands the sheet — and
`AMENDED_ADDITIONS` gets its first live entries in their place (`BENCH THEME`,
0 in the original and 1 in the amended; `FLASHCARDS`, 0 and 3).

⚠️ **The sheet carries three hardcoded fixture rows** — `TEACHER` → "Mr Badmus",
`TERM` → "Summer · Week 01 / 39", `EMAIL` → "ay@school.uk". All three are literal
text nodes, and "Mr Badmus" is already in `FIXTURE_TELLS`. They need real
`BINDINGS` before the sheet can ship. **`EMAIL` is a product question, not a
porting one** — nothing on the student surface shows an email today, and whether
a class page should is Mide's call, not a porter's.

⚠️ **`.stbtn` will collide.** Design's grafted style block already carries
`.stbtn[data-on="1"]{--st-bg:…;--st-ink:var(--pg-ground);--st-edge:…}` — LIVE token
names inside a foreign namespace. Inert today because nothing on the page carries
`.stbtn`; the picker's swatch buttons are what will carry it.

### For Mide — two things that are yours, not mine

**1. Chalk dissolves into the page, and that is Design's palette, not a bug.**
Chalk's bench `#EFE2CB` against the page ground `#FBF3E6` measures **1.16:1**, and
the docket `#FFFDF8` against that bench measures **1.26:1**. Both surfaces survive
on a hairline, the orange top rule and a shadow. Every contrast figure Design
states for chalk is met — this is about *surface separation*, which nothing
specifies. It reads as paper on paper where the other five read as a slab on a
page. Worth a look before the picker makes chalk reachable.

**2. The M1 dual-read fallback in `auth_user_has_scope` is still there** and its
own comment says *"removed at the final advisory flip"*. It is now harmless —
nothing a student can write reaches it — but whether M1 is finished is a schools
rollout call.

